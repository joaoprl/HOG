
public class HogOutput implements Runnable{
	
	Hog hog;
	double[][][] histograms;
	double[][][] output;
	
	public HogOutput(Hog hog, double[][][] histograms, double[][][] output){
		this.hog = hog;
		this.histograms = histograms;
		this.output = output;
	}

	@Override
	public void run() {
		for(int x = 0; x < image.getWidth() / 8 - 1; x++){
			for(int y = 0; y < image.getHeight() / 8 - 1; y++){
				int counter = 0;
				double mag = 0;
				for(int a = 0; a < 2; a++){
					for(int b = 0; b < 2; b++){
						for(int z = 0; z < 9; z++){
							output[x][y][counter] = histograms[x + a][y + b][z];
							mag += output[x][y][counter];
							counter++;				        				
						}
					}
				}

				if(mag != 0){
					for(int z = 0; z < 36; z++){
						output[x][y][z] /= mag;
					}
				}
			}
		}

	}

}
